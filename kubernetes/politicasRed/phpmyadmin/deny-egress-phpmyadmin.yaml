apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-egress
  namespace: phpmyadmin-namespace
spec:
  podSelector: {}    #esto hace que afecte a todos los pods del ns
  policyTypes:
    - Egress
  egress: []
